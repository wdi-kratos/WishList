<h2>Here is your List!</h2>

<h1>Wish List</h1>

<li><a href='/items/create'>Create Item</a></li>

<table>
  <thead>
    <th>
      Title
    </th>
    <th>
      Description
    </th>
    <th>
      <!-- <%= @type %> -->
    </th>
    <th>
      Edit?
    </th>
  </thead>

  <tbody>
    <!-- <% @items.each do |item| %>
      <tr>
        <td>
          <%= item.id %>
        </td>
        <td>
          <%= item.title %>
        </td>
        <td>
          <%= item.description %>
        </td>
        <td>
          <a href='/items/edit/<%= items.id %>'>Edit</a>
        </td>
      </tr>
    <% end %> -->
  </tbody>
</table>
